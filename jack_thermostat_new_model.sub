#!/bin/bash
#SBATCH -A CABRERA-SERRENHO-SL3-CPU
#SBATCH -p icelake
#SBATCH --mem=6840
#SBATCH -N 1
#SBATCH -t 00:30:00
#SBATCH -J p-jjjl-case-sweep
#SBATCH --array=0-5  # 2 zones * 3 patterns = 6 jobs

source ~/.bashrc

# Build parameter list
PARAMS=()
zones=(0 9)
heating_patterns=("once" "twice" "thrice")

for z in "${zones[@]}"; do
    for pattern in "${heating_patterns[@]}"; do
        PARAMS+=("--exp_typ thermostat \
        --zone $z \
        --heating_pattern $pattern \
        --algorithm rbc \
        --case 0 \
        --rep H28 \
        --wandb_name model_validation \
        --wandb_tags model_validation \
        --year 2023 \
        --comfort_temp_setpoint 20 \
        --setback_temp_setpoint 16 \
        --wandb_entity jjjl4 \
        --wandb_project jack-observations \
        --run_id True \
        --timesteps_per_hour 6 \
        --holiday True \
        --short_episode True \
        --inactivity_threshold 2 \
        --wandb_logging True \
        --no_ventilation True")
    done
done

# Run the job with singularity using the task ID as an index for PARAMS
singularity run cubes-cluster.simg python3.9 main_jack.py ${PARAMS[$SLURM_ARRAY_TASK_ID]}
